---
title: Home
description: Dollar DocSign is the $1/mo e-signature tool for founders and freelancers. Fast, simple, and built on the Documenso open-source stack.
---

import Image from 'next/image';
import Link from 'next/link';

import backgroundPattern from '@documenso/assets/images/background-pattern.png';
import cardBeautifulFigure from '@documenso/assets/images/card-beautiful-figure.png';
import cardConnectionsFigure from '@documenso/assets/images/card-connections-figure.png';
import cardPaidFigure from '@documenso/assets/images/card-paid-figure.png';
import cardSharingFigure from '@documenso/assets/images/card-sharing-figure.png';
import { cn } from '@documenso/ui/lib/utils';
import { Card, CardContent, CardTitle } from '@documenso/ui/primitives/card';

export const Wrapper = ({ children }) => {
  return <div className="mt-20">{children}</div>;
};

<Wrapper>
  <div
    className="fixed inset-0 -z-10 bg-center -mt-[15vh] h-full scale-125 object-cover dark:contrast-[70%] dark:invert dark:sepia md:scale-150 lg:scale-[175%] opacity-40 dark:opacity-20"
    style={{
      backgroundImage: `url('${backgroundPattern.src}')`,
    }}
  />
  <div className="flex flex-col items-center mb-16 text-center">
    <span className="text-4xl font-bold leading-tight tracking-tight md:text-[48px] lg:text-[64px]">
      Dollar DocSign – $1/mo signing for indie teams
    </span>
    <span className="text-muted-foreground mx-auto mt-4 max-w-3xl leading-normal tracking-tight">
      Ship contracts in minutes without paying enterprise pricing. Dollar DocSign keeps the full Documenso CE surface area but adds lightweight billing, usage limits, and founder-friendly pricing.
    </span>
    <div className="mt-6 flex flex-wrap items-center justify-center gap-4">
      <Link
        href="https://app.dollardocsign.com/signup"
        className="inline-flex items-center rounded-full bg-primary px-6 py-3 text-base font-semibold text-primary-foreground shadow hover:opacity-90"
      >
        Start for free
      </Link>
      <Link
        href="https://app.dollardocsign.com/login"
        className="inline-flex items-center rounded-full border border-border px-6 py-3 text-base font-semibold hover:bg-background/60"
      >
        View the app
      </Link>
    </div>
  </div>

  <div className="mt-6 grid grid-cols-1 md:grid-cols-2 gap-8 md:mt-8">
    <Link href="/users">
      <Card className="col-span-2 lg:col-span-1 h-full dark:bg-black/10 dark:border dark:border-stone-800">
        <CardContent className="grid grid-cols-1 gap-8 p-6 max-h-[460px]">
          <span className="text-foreground/80 leading-relaxed">
            <strong className="block text-2xl font-normal mb-2 tracking-tight">Learn</strong>
            Learn how to get started with your account and leverage our advanced features to level up your productivity.
          </span>
          <div className="flex items-center justify-center p-8 flex-grow">
            <Image
              src={cardBeautifulFigure}
              alt="its fast"
              className="w-full max-w-xs dark:contrast-[70%] dark:hue-rotate-180 dark:invert"
            />
          </div>
        </CardContent>
      </Card>
    </Link>

    <Link href='/developers'>
      <Card className="col-span-2 lg:col-span-1 h-full dark:bg-black/10 dark:border dark:border-stone-800">
        <CardContent className="grid grid-cols-1 gap-8 p-6 max-h-[460px]">
          <span className="text-foreground/80 leading-relaxed">
            <strong className="block text-2xl font-normal mb-2 tracking-tight">Build</strong>
            Everything you need to set up your local development environment.
            Use our API and utilize webhooks for seamless integration.
          </span>
          <div className="flex items-center justify-center p-8 flex-grow">
            <Image
              src={cardConnectionsFigure}
              alt="its fast"
              className="w-full max-w-sm dark:contrast-[70%] dark:hue-rotate-180 dark:invert"
            />
          </div>
        </CardContent>
      </Card>
    </Link>

  </div>
  <div className="mt-20 space-y-6">
    <h2 className="text-3xl font-bold text-center">
      <Trans>Plans built for founders &amp; freelancers</Trans>
    </h2>
    <p className="text-muted-foreground mx-auto max-w-2xl text-center text-base">
      <Trans>Every plan keeps the same secure signing experience. Pay only for the seats and document volume you actually need.</Trans>
    </p>
    <div className="grid gap-6 md:grid-cols-3">
      {[
        {
          name: 'Free',
          price: '$0 forever',
          description: 'Great for testing flows or sending a handful of agreements.',
          features: ['5 documents total', '1 user', 'Basic templates & signing'],
          cta: 'Start for free',
          href: 'https://app.dollardocsign.com/signup',
        },
        {
          name: 'Starter',
          price: '$1/mo',
          description: 'Upgrade once you need more than a lifetime quota.',
          features: ['20 documents / month', 'Priority email support', 'Import or build custom templates'],
          cta: 'Upgrade to Starter',
          href: 'https://app.dollardocsign.com/settings/billing',
        },
        {
          name: 'Pro / Teams',
          price: '$11/mo',
          description: 'For teams that collaborate on contracts every week.',
          features: [
            '100 documents / month',
            'Up to 5 team members',
            'Shared templates & workflows',
            'Team-level billing portal',
          ],
          cta: 'Upgrade to Pro',
          href: 'https://app.dollardocsign.com/settings/billing',
        },
      ].map((plan) => (
        <Card
          key={plan.name}
          className={cn(
            'flex h-full flex-col border border-border/60',
            plan.name === 'Starter' && 'border-primary shadow-lg',
          )}
        >
          <CardContent className="flex h-full flex-col gap-4 p-6">
            <div>
              <p className="text-sm uppercase tracking-wide text-muted-foreground">{plan.name}</p>
              <p className="text-2xl font-semibold">{plan.price}</p>
              <p className="text-muted-foreground mt-2 text-sm">{plan.description}</p>
            </div>
            <ul className="space-y-2 text-sm">
              {plan.features.map((feature) => (
                <li key={feature} className="flex items-start gap-2">
                  <span className="text-primary mt-0.5">•</span>
                  <span>{feature}</span>
                </li>
              ))}
            </ul>
            <div className="mt-auto">
              <Link
                href={plan.href}
                className={cn(
                  'inline-flex w-full items-center justify-center rounded-full px-4 py-2 text-sm font-semibold',
                  plan.name === 'Starter'
                    ? 'bg-primary text-primary-foreground'
                    : 'border border-border text-foreground hover:bg-background/70',
                )}
              >
                {plan.cta}
              </Link>
            </div>
          </CardContent>
        </Card>
      ))}
    </div>
    <p className="text-center text-xs text-muted-foreground">
      Built proudly on the open-source Documenso Community Edition.
    </p>
  </div>
</Wrapper>
